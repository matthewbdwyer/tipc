main() {
  var x;
  x = check(7);		// output 7
  x = check(0);		// output 0
  x = check(-7);	// error -7
  return 3;		// unreachable since error exits
}

check(z) {
  if (0 > z)
     error z;
  else 
     output z;
  return 42;
}
