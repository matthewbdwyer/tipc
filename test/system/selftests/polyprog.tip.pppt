ident(p) 
{
  return *p;
}

main() 
{
  var n;
  n = alloc 42;
  if ((ident(n) != 42)) 
    error ident(n);
  return 0;
}

Functions : {
  ident : (⭡int) -> int,
  main : () -> int
}

Locals for function ident : {
  p : ⭡int
}

Locals for function main : {
  n : ⭡int
}
